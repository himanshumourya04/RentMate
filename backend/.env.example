# Backend Environment Variables
# Copy this to .env and fill in your actual values

MONGO_URI=mongodb+srv://himanshu7046m_db_user:himanshu7046m@cluster0.jonvis7.mongodb.net/?appName=Cluster0
JWT_SECRET=rentmate_super_secret_key_2024
PORT=5000

# Comma-separated list of allowed frontend origins (add your Vercel URL here after deploying)
CLIENT_URL=http://localhost:5175,rent-mate-puce.vercel.app

# Firebase (for OTP during signup)
FIREBASE_PROJECT_ID=your_project_id

# ─── Cloudinary (for persistent image storage) ───────────────────────────────
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ─── Render (auto-set by Render — used for keep-alive ping) ──────────────────
# Render automatically sets RENDER_EXTERNAL_URL to your service's public URL.
# You do NOT need to set this manually.
# RENDER_EXTERNAL_URL=https://your-app.onrender.com
